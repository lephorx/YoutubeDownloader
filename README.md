First install the yt-dlp package with:

```Bash
pip install yt-dlp
```
second create a links.txt file and insert your desired youtube videos

third create a folder downloads or change the folder path of the downloads

finally run script with

```Bash
python main.py
```